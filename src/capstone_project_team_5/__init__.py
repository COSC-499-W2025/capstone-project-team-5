def main() -> int:
    """Entry point for the application.

    Returns:
        Exit code (0 for success, non-zero for failure).
    """
    from capstone_project_team_5.cli import main as cli_main

    return cli_main()
